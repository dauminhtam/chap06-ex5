#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int a = 80;
	char b = 'C';
	printf ("\nThis is number stored in 'a' %d", a);
	printf ("\nThis is charater interpreted from 'a' %c", a);
	printf ("\nThis is charater stored in 'C' %c", b);
	printf ("\nThis is number stored in 'C' %d", b);
	return 0;
}
